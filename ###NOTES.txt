###NOTES — Codebase Review (2026-06-14)
==========================================

PROBLEMS (Must Fix)
-------------------
[P1] README.md links to docs/manual.md and docs/documentation.md — neither file exists (broken links).
[P2] notes.md is stale — references "Gupshup", lists routes as "/", says "Backend initialized (FastAPI stub)". App is in production.
[P3] Migration drift — models.py defines onboarding_completed, UniqueConstraints (feedbacks, coupons), CheckConstraint (subscription_status) that are NOT in any migration SQL. If prod DB was built from migrations, these may be missing.
[P4] Timezone mismatch — 0000_init_schema.sql uses TIMESTAMP WITHOUT TIME ZONE; supabase_setup.sql uses WITH TIME ZONE.
[P5] main.py global exception handler returns full traceback in JSON response — should be disabled in production.
[P6] docs/architecture.md has duplicate "## 6" section numbering. docs/operations/deployment.md has duplicate "## 5".
[P7] billing/create-order returns a dummy order ID — Razorpay order creation is not wired up yet.

SUGGESTIONS
-----------
[S1] Centralize MarketingPage API calls into api.ts — currently two inline fetch() calls break the pattern.
[S2] Add a 404 catch-all route in App.tsx — currently no fallback for unknown paths.
[S3] Extract background animations (Aurora/Papyrus) into a shared layout component — currently duplicated across LandingPage, ContactPage, PrivacyPage, TermsPage.
[S4] docs/architecture/database.md is missing ProcessedWebhook table and onboarding_completed column.
[S5] docs/operations/runbook.md is skeletal — needs monitoring, alerting, incident response, backup/restore.
[S6] No documentation exists for the Supabase keep-alive edge function or its cron scheduling.
[S7] No documentation exists for the supabase_setup.sql trigger (handle_new_user) — the auto-provisioning flow.
[S8] audit.py commits independently — could conflict if called mid-transaction. Consider deferring commit to caller.
[S9] verify_coupon.py test uses plaintext passcode comparison instead of bcrypt hash — test setup doesn't match prod behavior.
[S10] todos.txt is empty — consider removing or populating it.

OBSERVATIONS
------------
[O1] Dual auth system is well-designed: Supabase JWT for owners, scoped custom JWT for customers, bcrypt passcode for staff.
[O2] Idempotency is handled at every critical path: feedback (unique + row lock), webhooks (ProcessedWebhook dedup), payments (audit log search).
[O3] NullPool + serverless architecture is correctly implemented for Vercel cold starts.
[O4] WhatsApp webhook has proper HMAC-SHA256 verification. Razorpay webhook also verifies signatures.
[O5] Rate limiting is well-applied to public endpoints (10/min feedback links, 5/min submit, 3/hr contact).
[O6] Design system docs (aurora-design.md, papyrus-design.md, minimalist-design.md) are excellent quality.
[O7] security.md describes a hardened target state — verify scratch.txt vulnerabilities are actually patched in code.
[O8] scratch.txt is an invaluable security trace document — should be preserved as a reference.
